# prw3

cd to a project in the desired part (e.g. `cd part1/anecdotes`)

run `npm i`

run `npm run dev`

if needed, run `npx json-server --watch db.json --port 3001`

website should be running/working, if not then look it up.
